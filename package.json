{
  "name": "insureflow-pro-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "website",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w website"
  },
  "engines": {
    "node": "22.x"
  }
}
